# Kirana Desk Backend

Separate Node.js application for Kirana Desk licensing, sync, and admin APIs.

## Setup

```bash
cd kirana-desk-backend
npm install
copy .env.example .env
npm run dev
```

The API starts on `http://localhost:3000` by default.

## Structure

- `src/` - Express API, middleware, models, routes, and services
